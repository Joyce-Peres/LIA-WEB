<!-- Epic 1 Retrospective (modo local) -->
# Retrospectiva — Epic 1: Setup e Autenticação (modo local)

Data: 2025-12-21  
Epic: 1 — Setup e Autenticação  
Escopo: Stories 1.1 → 1.5

## ✅ O que foi entregue

- Base React + TS + Vite + Tailwind com rotas (Story 1.1).
- Pivot para **MVP 100% local** (sem serviços externos/pagos) com:
  - Login local + sessão (`src/lib/auth.ts`)
  - Perfil local (“LIA”) com avatar e métricas (XP/streak) (`src/lib/profile.ts`, `/profile`)
  - Persistência de sessão com validade (`expiresAt`) + restore automático (Story 1.5)
- Testes unitários mínimos (Vitest) para auth/profile.

## 👍 O que funcionou bem

- **Decisão de pivot** reduziu risco de custo e removeu bloqueios de setup externo.
- **Arquitetura simples** (localStorage + validação) permitiu evolução rápida.
- **Quality gates** (test/lint/build) rodando de forma previsível.

## ⚠️ Pontos de atenção

- `localStorage` pode falhar (quota/blocked); temos fallback de sessão em memória, mas persistência de progresso ainda depende do storage.
- Modelo atual assume um único usuário “lia” (ok para MVP; limita multiusuário).
- Ainda não há testes E2E (aceitável nesta fase, mas recomendado quando começarem fluxos de UI mais complexos).

## 🔧 Ações recomendadas (próximos épicos)

- Definir quando migrar de `localStorage` para `IndexedDB` para progresso (mais robusto).
- Considerar uma camada de “repository” local com interface (para trocar storage no futuro sem refatorar UI).
- Planejar E2E com Playwright para fluxos críticos (login/rota protegida/navegação).


